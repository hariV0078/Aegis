# Midnight Scaffold

This folder contains the Midnight integration scaffold for PrivacyForge.

Current status:

- The backend stores Midnight proof fields on each agent run.
- The UI can display a proof badge and poll for updates.
- A real Midnight testnet submission bridge still needs the deployed contract address and signing setup.

Files:

- `contracts/AuditLog.compact` - Compact contract scaffold for privacy-proof anchoring.